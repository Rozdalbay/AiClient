<UserControl x:Class="AiDesktopClient.Controls.GeneratingIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="34"
             Height="16"
             SnapsToDevicePixels="True">
    <UserControl.Resources>
        <Storyboard x:Key="TypingStoryboard">
            <DoubleAnimation Storyboard.TargetName="Dot1"
                             Storyboard.TargetProperty="Opacity"
                             From="0.25" To="1"
                             Duration="0:0:0.45"
                             BeginTime="0:0:0"
                             AutoReverse="True"
                             RepeatBehavior="Forever"/>
            <DoubleAnimation Storyboard.TargetName="Dot2"
                             Storyboard.TargetProperty="Opacity"
                             From="0.25" To="1"
                             Duration="0:0:0.45"
                             BeginTime="0:0:0.15"
                             AutoReverse="True"
                             RepeatBehavior="Forever"/>
            <DoubleAnimation Storyboard.TargetName="Dot3"
                             Storyboard.TargetProperty="Opacity"
                             From="0.25" To="1"
                             Duration="0:0:0.45"
                             BeginTime="0:0:0.30"
                             AutoReverse="True"
                             RepeatBehavior="Forever"/>
        </Storyboard>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal"
                HorizontalAlignment="Left"
                VerticalAlignment="Center">
        <Ellipse x:Name="Dot1"
                 Width="5"
                 Height="5"
                 Margin="0,0,4,0"
                 Fill="{DynamicResource PrimaryAccentBrush}"
                 Opacity="0.25"/>
        <Ellipse x:Name="Dot2"
                 Width="5"
                 Height="5"
                 Margin="0,0,4,0"
                 Fill="{DynamicResource PrimaryAccentBrush}"
                 Opacity="0.25"/>
        <Ellipse x:Name="Dot3"
                 Width="5"
                 Height="5"
                 Fill="{DynamicResource PrimaryAccentBrush}"
                 Opacity="0.25"/>
    </StackPanel>
</UserControl>
